<div class="form-area col-sm-4 col-sm-offset-4">
  <center><h1><%= t("user.label.user") %></h1></center>  
  <div class="row">
    <div class="col-sm-3">
      <div class="gravatar_edit">
        <%= gravatar_for @user %>        
      </div>
    </div>
    <div class="col-sm-9">
      <%= t("user.label.username") %> <%= @user.username %><br>
      <%= t("user.label.email") %> <%= @user.email %><br>
    </div>
  </div>
</div>
